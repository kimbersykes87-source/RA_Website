# Cloudflare Workers configuration
name = "email-tracking-worker"
main = "worker-bundled.js"
compatibility_date = "2024-01-01"

# Account settings
account_id = "503a8a2ff4270cb830d68d08e3705e92"

# Custom domain - commented out for now to test workers.dev
# routes = [
#   { pattern = "track.rubberarmstrong.com/*", zone_name = "rubberarmstrong.com" }
# ]

# Enable workers.dev route
workers_dev = true

# Environment variables (secrets set via CLI)
# wrangler secret put GOOGLE_SERVICE_ACCOUNT_JSON
# wrangler secret put SHEET_ID

[build]
command = ""

[env.production]
name = "email-tracking-worker"
vars = { ENVIRONMENT = "production" }

[env.staging]
name = "email-tracking-worker-staging"
vars = { ENVIRONMENT = "staging" }

